{
  "name": "hello-go",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start:docker": "make build && node ./node_modules/.bin/serverless offline start --useDocker",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "serverless": "^1.82.0",
    "serverless-go-build": "0.0.6",
    "serverless-offline": "^6.7.0"
  }
}
